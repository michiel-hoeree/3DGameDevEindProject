using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class boot_bewegen : MonoBehaviour
{
    // Start is called before the first frame update
    public float movement_forward;
    public float movement_left_right;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
